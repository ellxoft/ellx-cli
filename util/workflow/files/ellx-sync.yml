name: Ellx sync

on:
  push:
    branches:
      - master

jobs:
  test-client:
    name: Sync
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v1
      - uses: matyunya/ellx-sync@1
        with:
          ellxUrl: https://api.ellx.io
          key: {{ secrets.ELLX_KEY }}
